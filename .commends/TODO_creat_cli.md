***READ ONLY FILE**  
task: Build complete reusable agentic workspace package with standardized operational context.



Build complete reusable agentic workspace package with standardized operational context.



Create a fully reusable, stack-agnostic agentic workspace boilerplate package.

Include all rules, agents, skills, workflows, scripts, configs, markdown, and JSON files.

Automatically detect and convert project-specific logic into generic reusable templates.

Preserve complete repository structure, relationships, references, and operational workflows.

Replace project-specific names, paths, identifiers, and variables with dynamic placeholders.

Update inter-file references, imports, paths, commands, and dependencies to generic mappings.

Support installation, population, and synchronization on both new and existing repositories.

Ensure all placeholders, references, and generated configurations resolve automatically during setup.





Goal: Create a portable, self-contained agentic workspace system enabling rapid, standardized project initialization across any compatible repository.





provide a CLI bootstrap tool for resua on other projects. 

5 iterations for the final results and validate full and zero missing or incomplate 

list everything added at the end in one line = name list when done in the conversation 





