**READ ONLY FILE ****

Do the following tasks:
- [ ] read all project test related rules.
- [ ] list all releated tests files.
- [ ] comper to plan implementations files and determent the following:
      - if and what needs to be adjusted in existing tests files due to recent implementations.
      - if and what tests needs to be added to fully cover recent implemntations.
- [ ] if tests files needs adjustemdts and or if need new tests then plan and implement existing tests adjustems and create new tests.
- [ ] run all relevant tests localy and resolve any issue if existing.
